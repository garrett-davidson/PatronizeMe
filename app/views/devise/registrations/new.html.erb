<p id="notice"><%= notice %></p>
<%= form_for(@user, url:registration_path(@user)) do |f| %>
<div class="container">
    <h1>Finish Registration <%=@user.username%></h1>
    <hr>
    <div class="row">
        <div class="col-md-9 personal-info">
            <form class="form-horizontal" role="form">
                <div class="form-group">
                    <label class="col-lg-3 control-label">Full Name:</label>
                    <div class="col-lg-8">
                        <%= f.text_field :name, class: 'form-control'%>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-lg-3 control-label">Email Address:</label>
                    <div class="col-lg-8">
                        <%= f.text_field :email, class: 'form-control' %>
                    </div>
                </div>
                <div class="form-group">
                    <label class="col-md-3 control-label"></label>
                    <div class="col-md-8">
                        <%= f.submit "Save changes", class: "btn btn-large btn-primary", action: :post %>
                        <a href="/" class="btn btn-primary active" role="button" aria-pressed="true">Cancel</a>
                    </div>
                </div>
              <%= f.hidden_field :provider, class: 'p' %>
              <%= f.hidden_field :uid, class: 'p' %>
              <%= f.hidden_field :email, class: 'p' %>
              <%= f.hidden_field :username, class: 'p' %>
              <%= f.hidden_field :name, class: 'p' %>
            </form>
        </div>
    </div>
</div>
<hr>
<% end %>
